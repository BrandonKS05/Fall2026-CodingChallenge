# Workspace packages. Root scripts fan out to these with `pnpm -r`.
packages:
  - frontend
  - backend
  - shared
  # Python, and no JavaScript in it: the manifest exists so `pnpm dev` starts
  # the assistant alongside the app instead of leaving the widget unanswered.
  - services/wumbo-ai
