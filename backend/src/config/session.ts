/** Session cookie settings shared by the token service and the cookie helpers, so the two can never disagree. */
export const SESSION_COOKIE_NAME = 'trove_session';
export const SESSION_TTL_SECONDS = 7 * 24 * 60 * 60;
